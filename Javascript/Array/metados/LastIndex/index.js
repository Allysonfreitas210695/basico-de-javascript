/*
O método lastIndexOf() retorna o ultimo índice que um certo elemento pode ser encontrado no array,
 ou -1 se o elemento não estiver presente.
 O array é pesquisado de trás para frente, começando pelo fromIndex.
 */

const vetor = [1,2,3,4,5,6,8,2];

const numeroEscolhido = 2;

console.log(vetor.lastIndexOf(numeroEscolhido));

