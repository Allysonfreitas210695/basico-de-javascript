//O método reverse() inverte os itens de um array. O primeiro elemento do array se torna o último e o último torna-se o primeiro.

const vetor = [1,2,3,4,5,6,7,8,9,9,0,13];

const inverso = vetor.reverse();

console.log(inverso);
